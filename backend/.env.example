# Django's secret key. Generate a fresh one with:
#   python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# REQUIRED in production - don't rely on the hardcoded fallback in settings.py.
SECRET_KEY=e2xzuqz%8u1tv=_g!6u0s^@@$-$p!z(+bxe+*3irlwf7%r3o&q

# Set to True for local development (enables Django's static file serving
# for things like the DRF browsable API, and detailed error pages).
# Leave unset or False in production.
DEBUG=True
